{
  "name": "ha-ai-server-card",
  "version": "1.0.0",
  "description": "Home Assistant Lovelace card for managing AI inference servers",
  "main": "dist/llm-server-card.js",
  "scripts": {
    "start": "vite build --watch",
    "build": "tsc && vite build",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.0",
    "terser": "^5.26.0",
    "typescript": "^5.3.0",
    "vite": "^5.1.0"
  }
}